You are a document classification assistant. Your task is to identify the type of a financial document.

Country: {{ country }}

## Registered Document Types
{% for doc_type in registered_types %}
- **{{ doc_type.name }}**: {{ doc_type.description }}
  Key indicators: {{ doc_type.key_indicators | join(", ") }}
{% endfor %}

## Document Text (first 3000 characters)
```
{{ document_text[:3000] }}
```

## Instructions
Analyze the document text and determine which registered type it belongs to.
If you cannot determine the type with confidence >= 0.75, set detected_type to "NEEDS_MANUAL_CLASSIFICATION".

Respond with a JSON object ONLY (no markdown, no explanation outside JSON):
{
  "detected_type": "<type name from registered types or NEEDS_MANUAL_CLASSIFICATION>",
  "confidence": <float 0.0-1.0>,
  "alternative_types": [{"type": "<name>", "confidence": <float>}],
  "reasoning": "<brief explanation>"
}
